#ansible all -m raw -a "ls;data;uptime" => To run multiple commands
#ansible all -m command -a "apt install cowsay -y" => To run a single command
#ansible all -m command -a "apt update -y" -k => To login with the password
#make sure you do the host_key_checking = false
#ansible-doc -l => To list the modules
#ansible-doc copy => Help on the copy module
#ansible db -m copy -a 'src=/etc/passwd dest=/tmp'
#ansible db -m copy -a 'src=/etc/passwd dest=/tmp owner=aws group=azure mode=777'
#ansible db -m copy -a 'src=/etc/passwd dest=/tmp owner=aws group=azure mode=777 backup=yes'
#ansible all -m copy -a 'src=/etc/passwd remote_src=yes dest=/tmp owner=aws group=azure mode=777 backup=yes' => To run on the target or remote machine
#ansible all -m fetch -a 'src=/etc/passwd dest=/logsans'
#ansible db -m fetch -a 'src=/etc/passwd dest=/logsans flat=yes'
#ansible all -m file -a 'path=/tmp/india state=directory'
#ansible all -m file -a 'path=/tmp/india state=touch'
#ansible all -m file -a 'path=/tmp/india/delhi state=touch mode=777'
#ansible all -m file -a 'path=/tmp/india/delhi state=absent'
#ansible all -m file -a 'src=/etc/passwd dest=/tmp/newlink state=link'
#ansible all -m file -a 'src=/etc/passwd dest=/tmp/newlink state=hard'
#ansible all -m shell -a 'sh /tmp/myscript.sh'
#ansible all -m package -a "name=apache2 state=absent" => to install state=present
#ansible-doc -l | grep yum => yum , yum_repository
#installing using the yum , apt and there repos respectively.
#service module used to manage a service on a remote server.
#group module => Manage presence of groups on a host.
#user module => Manage user on the host.
#Lineinfile module This module ensures a particular line is in a file, or replace an existing line using a back-referenced regular expression.
